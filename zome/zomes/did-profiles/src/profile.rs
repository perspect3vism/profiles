use did_doc::Document;
use hdk3::prelude::*;

use crate::{CreateProfileInput, UpdateProfileInput, DidInput, RegisterDidInput, AddProfile};

pub fn create_profile(create_data: CreateProfileInput) -> ExternResult<()> {
    Ok(())
}